from __future__ import annotations

# Phase 3: Full PII redaction
# Phase 0 stub — no-op passthrough


def redact_email_body(body: str) -> str:
    return body


def redact_subject(subject: str) -> str:
    return subject
